import React from "react";
import { CategoryForm } from "../CategoryForm";

export const metadata = {
  title: "Create Collection | Ayra Admin",
};

export default function NewCategoryPage() {
  return <CategoryForm />;
}
